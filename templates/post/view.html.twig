{% extends 'originedanse.html.twig' %}

{% block title %}{{ post.title}}{% endblock %}

{% block body %}
<article>
    <div>
            <figure><img src="{{ asset('originedanse/images/rize.jpg') }}" alt="affiche film rize"></figure>
            <h2>{{ post.title }}</h2>
            <p>{{ post.content }}
            </p>
    </div>
    </article>
{% endblock %}
